# 1.查询： 列表.index(元素)
# 如果被查找的元素不存在，会报错
# 2.修改： 列表[下标]=值
# 3.插入： 列表.insert(下标，元素) 在指定的下标位置插入
# 4.追加： 列表.append(元素) 插入尾部
# 5.追加一批： 列表.extend(其他数据容器)
# 6.删除，通过下标： del 列表[下标]  列表.pop(下标) 方法
                       # pop可以把删除的元素得到
# 7.删除，通过内容  列表.remove(元素) 只能删从上到下找到的第一个
# 8.清空： 列表.clear()
# 9.统计数量： 列表.count(元素)
# 10.统计元素： len(列表)

# 允许重复数据存在




